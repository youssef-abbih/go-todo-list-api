apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  selector:
    db: go-todo-list-api-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
  clusterIP: None